version: '3'

services:
  webserver:
    image: trupti660/image:v1
    ports:
      - "81:80"
    environment:
      - department=dev

  backend:
    image: trupti660/image:alpine     
    ports:
      - "8080:80"